// Escucha el evento 'DOMContentLoaded', que se dispara cuando el contenido del DOM
// ha sido completamente cargado y parseado, pero antes de que se carguen imágenes y otros recursos.
document.addEventListener('DOMContentLoaded', () => {
  // Muestra un mensaje en la consola indicando que la aplicación está cargada.
  console.log('Conversor de Divisas y Criptomonedas cargado');
});
